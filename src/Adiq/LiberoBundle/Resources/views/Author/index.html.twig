{% extends 'AdiqLiberoBundle::layout.html.twig' %}

{% block title %}Autorzy{% endblock %}


{% block content %}

    <h1 class="title">Panel: Autorzy</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Imię</th>
                <th>Nazwisko</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.name }}</td>
                <td>{{ entity.lastname }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('author_edit', { 'id': entity.id }) }}">edytuj dane autora</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('author_new') }}">
                Dodaj nowego autora
            </a>
        </li>
    </ul>

{% endblock %}
